# Test Note

This is my first test note on GitHub Pages.

## Commands

- `whoami`  
  Show the current user.

- `id`  
  Show the current user ID, group ID, and group memberships.

- `pwd`  
  Print the current working directory.

## Notes

- This page is written in Markdown.
- GitHub Pages turns this `.md` file into a webpage.
- If this page loads, the basic site is working.

## DIY Quiz

1. Run `whoami`.
2. Run `id`.
3. Run `pwd`.
4. Create a new Markdown file.
5. Link to it from `index.md`.
